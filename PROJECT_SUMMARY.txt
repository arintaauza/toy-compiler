================================================================================
                    LUNA COMPILER - PROJECT SUMMARY
================================================================================

PROJECT STATUS: COMPLETE (All 8 Phases Implemented)

Location: /Users/arinta/Documents/Arinta-Work/Code Claude/compiler_project
Tests: 390 passing

================================================================================
WHAT IS LUNA?
================================================================================

Toy is a statically-typed, C-like programming language with:

- Static typing: int, float, bool, string, void
- Variables: let x: int = 42;
- Functions: fn add(a: int, b: int) -> int { return a + b; }
- Control flow: if/else, while loops
- Operators: +, -, *, /, %, ==, !=, <, >, <=, >=, &&, ||, !
- Built-in: print() function
- Comments: // single-line, /* multi-line */

Example:
```toy
fn fibonacci(n: int) -> int {
    if n <= 1 { return n; }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

fn main() -> int {
    print(fibonacci(10));  // Prints: 55
    return 0;
}
```

================================================================================
COMPILER ARCHITECTURE
================================================================================

Source Code (.toy)
       |
       v
[Phase 1] Lexer -----------------> Tokens
       |
       v
[Phase 2] Parser ----------------> Abstract Syntax Tree (AST)
       |
       v
[Phase 3] Semantic Analyzer -----> Type-checked AST + Symbol Table
       |
       v
[Phase 4] IR Generator ----------> SSA-based IR + Control Flow Graph
       |
       v
[Phase 5] Optimizer -------------> Optimized IR
       |
       +---> [Phase 6] x86-64 Codegen ---> Native Binary
       |
       +---> [Phase 7] LLVM Codegen -----> LLVM IR ---> Native Binary

[Phase 8] CLI Tool (toy.py) - Professional command-line interface

================================================================================
PROJECT STRUCTURE
================================================================================

compiler_project/
├── toy.py                    # CLI tool - main entry point
├── README.md                  # Project overview
├── requirements.txt           # Python dependencies (pytest, llvmlite)
│
├── docs/
│   ├── LANGUAGE_SPEC.md       # Toy language specification
│   ├── COMPILER_DESIGN.md     # Architecture overview
│   ├── COMPILER_INTERNALS.md  # Type system, IR, optimizations
│   └── BUILD_PHASES.md        # Implementation roadmap (completed)
│
├── src/
│   ├── lexer/
│   │   ├── token.py           # TokenType enum, Token class
│   │   └── lexer.py           # Lexical analyzer
│   │
│   ├── parser/
│   │   ├── ast_nodes.py       # AST node classes, visitor pattern
│   │   └── parser.py          # Recursive descent parser
│   │
│   ├── semantic/
│   │   ├── types.py           # Type system (ToyType, PrimitiveType)
│   │   ├── symbol_table.py    # Symbol table with scope chain
│   │   ├── type_checker.py    # Type validation
│   │   └── semantic_analyzer.py  # Main semantic analysis
│   │
│   ├── ir/
│   │   ├── instructions.py    # IR instruction classes
│   │   ├── cfg.py             # Control flow graph
│   │   ├── ir_generator.py    # AST to SSA IR translation
│   │   ├── ir_printer.py      # IR pretty printing
│   │   ├── ssa.py             # SSA utilities
│   │   └── optimizations/
│   │       ├── pass_manager.py       # Optimization framework
│   │       ├── constant_folding.py   # 2+3 -> 5
│   │       ├── dead_code_elimination.py
│   │       ├── cse.py                # Common subexpression elimination
│   │       ├── copy_propagation.py
│   │       ├── algebraic.py          # x*1 -> x
│   │       └── control_flow.py       # Branch elimination
│   │
│   ├── codegen/
│   │   ├── stack_frame.py     # Stack frame layout
│   │   ├── x86_64_codegen.py  # x86-64 AT&T syntax generator
│   │   ├── asm_emitter.py     # Assembly file generation
│   │   ├── llvm_codegen.py    # LLVM IR generator
│   │   └── llvm_emitter.py    # LLVM JIT compilation
│   │
│   └── utils/
│       └── error.py           # Error classes
│
├── tests/
│   ├── test_lexer.py          # 33 tests
│   ├── test_parser.py         # 63 tests
│   ├── test_semantic.py       # 65 tests
│   ├── test_ir.py             # 58 tests
│   ├── test_optimizations.py  # 52 tests
│   ├── test_codegen.py        # 39 tests
│   ├── test_llvm_codegen.py   # 41 tests
│   └── test_integration.py    # 39 tests
│
└── examples/
    ├── hello_world.toy       # Basic I/O
    ├── fibonacci.toy         # Recursion
    ├── factorial.toy         # Recursion
    ├── fizzbuzz.toy          # Conditionals
    ├── prime_checker.toy     # Prime detection
    ├── gcd.toy               # GCD/LCM algorithms
    ├── power.toy             # Fast exponentiation
    ├── sum_of_digits.toy     # Digit manipulation
    ├── collatz.toy           # Collatz conjecture
    └── triangle.toy          # Triangle numbers

================================================================================
USAGE
================================================================================

# Compile and run a Toy program
python toy.py examples/fibonacci.toy

# With optimizations
python toy.py -O examples/fibonacci.toy

# Show compilation stages
python toy.py --tokens examples/hello_world.toy    # Show tokens
python toy.py --ast examples/hello_world.toy       # Show AST
python toy.py --ir examples/hello_world.toy        # Show IR
python toy.py --ir-opt examples/hello_world.toy    # Show optimized IR
python toy.py --asm examples/hello_world.toy       # Show x86-64 assembly
python toy.py --llvm examples/hello_world.toy      # Show LLVM IR

# Generate output file
python toy.py -o output.s --asm examples/fibonacci.toy

# Verbose mode (shows timing stats)
python toy.py -v examples/fibonacci.toy

# Use LLVM backend
python toy.py --backend llvm examples/fibonacci.toy

================================================================================
TEST RESULTS
================================================================================

Total: 390 tests passing

  Phase 1 - Lexer:        33 tests
  Phase 2 - Parser:       63 tests
  Phase 3 - Semantic:     65 tests
  Phase 4 - IR:           58 tests
  Phase 5 - Optimizations: 52 tests
  Phase 6 - x86-64:       39 tests
  Phase 7 - LLVM:         41 tests
  Phase 8 - Integration:  39 tests

Run tests:
  python -m pytest                    # All tests
  python -m pytest tests/test_lexer.py -v   # Specific phase

================================================================================
IMPLEMENTATION HIGHLIGHTS
================================================================================

Type System:
- Static typing with int, float, bool, string, void
- Type checking for all operations
- No implicit type conversions

SSA-based IR:
- Each variable assigned exactly once
- Versioned variables (x_0, x_1, x_2)
- Phi functions at control flow joins

Optimizations:
- Constant folding (2+3 -> 5)
- Dead code elimination
- Common subexpression elimination
- Copy propagation
- Algebraic simplification (x*1 -> x)
- Control flow optimization

Code Generation:
- x86-64 AT&T syntax (macOS compatible)
- System V AMD64 ABI
- LLVM IR via llvmlite with JIT execution

================================================================================
DEPENDENCIES
================================================================================

Required:
  - Python 3.8+
  - pytest (for tests)
  - llvmlite (for LLVM backend)

Install:
  pip install -r requirements.txt

================================================================================
